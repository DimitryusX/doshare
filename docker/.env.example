# Monorepo root path (absolute, e.g. /srv/stacks/doshare)
PROJECT_PATH=/PATH-TO-PROJECT
NODE_VERSION=20

# Server
SERVER_HOST=https://your-domain.com
SERVER_PORT=3003

# Uploads
UPLOAD_LIMIT=125829120
UPLOAD_FOLDER=/PATH-TO-UPLOAD-FOLDER
UPLOADS_PATH=/PATH-TO-UPLOADS

# Client (inside container)
CLIENT_FOLDER=/PATH-TO-CLIENT-FOLDER

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DATABASE=0

# Optional: Dockerfile path relative to server context (default: ../docker/Dockerfile)
# SERVER_DOCKERFILE=../docker/Dockerfile

# Optional: Sentry
# SENTRY_DSN=
# SENTRY_TRACES_SAMPLE_RATE=1.0
# SENTRY_PROFILES_SAMPLE_RATE=1.0
